# Staging build example — copy to .env.staging then:
#   npm run build -- --mode staging
#
# Local development should keep using .env (localhost:8000).

VITE_COMPOSITOR_API_URL=https://compositor-test.example.com/api/v1
